<VirtualHost *:80>
	ServerName backend.base.lc

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/html/backend/web/

	ErrorLog ${APACHE_LOG_DIR}/base-error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	<Directory /var/www/html/backend/web/ >
        Options FollowSymLinks

        AllowOverride All
        Order deny,allow
		allow from all

		Require all granted
	</Directory>
</VirtualHost>

<VirtualHost *:80>

    ServerName api.base.lc

    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/api/web

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    <Directory /var/www/html/api/web >
            Options FollowSymLinks

            AllowOverride All
            Order deny,allow
            allow from all

            Require all granted
    </Directory>
</VirtualHost>

<VirtualHost *:80>

    ServerName base.lc
	ServerAlias www.base.lc

    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/frontend/web

    ErrorLog ${APACHE_LOG_DIR}/base-error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    <Directory /var/www/html/frontend/web >
            Options FollowSymLinks

            AllowOverride All
            Order deny,allow
            allow from all

            Require all granted
    </Directory>
</VirtualHost>


<VirtualHost *:80>

    ServerName static.base.lc

    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/static

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    <Directory /var/www/html/static >
            Options FollowSymLinks

            AllowOverride All
            Order deny,allow
            allow from all

            Require all granted
    </Directory>
</VirtualHost>
